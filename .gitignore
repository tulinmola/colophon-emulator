# Local working notes and raw research — not part of the published record.
# Sources get cited at their point of use, in the code and docs that use them.
notes/

# Build output.
build/

# Finder droppings.
.DS_Store

# Downloaded test data (see tools/fetch-tests.sh).
test/data/
